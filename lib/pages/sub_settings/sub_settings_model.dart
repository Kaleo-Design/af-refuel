import '/all_components/nav/nav_link_title/nav_link_title_widget.dart';
import '/flutter_flow/flutter_flow_util.dart';
import '/index.dart';
import 'sub_settings_widget.dart' show SubSettingsWidget;
import 'package:flutter/material.dart';

class SubSettingsModel extends FlutterFlowModel<SubSettingsWidget> {
  ///  State fields for stateful widgets in this page.

  // Model for nav_link_title component.
  late NavLinkTitleModel navLinkTitleModel1;
  // Model for nav_link_title component.
  late NavLinkTitleModel navLinkTitleModel2;
  // Model for nav_link_title component.
  late NavLinkTitleModel navLinkTitleModel3;
  // Model for nav_link_title component.
  late NavLinkTitleModel navLinkTitleModel4;
  // Model for nav_link_title component.
  late NavLinkTitleModel navLinkTitleModel5;
  // Model for nav_link_title component.
  late NavLinkTitleModel navLinkTitleModel6;
  // Model for nav_link_title component.
  late NavLinkTitleModel navLinkTitleModel7;
  // Model for nav_link_title component.
  late NavLinkTitleModel navLinkTitleModel8;

  @override
  void initState(BuildContext context) {
    navLinkTitleModel1 = createModel(context, () => NavLinkTitleModel());
    navLinkTitleModel2 = createModel(context, () => NavLinkTitleModel());
    navLinkTitleModel3 = createModel(context, () => NavLinkTitleModel());
    navLinkTitleModel4 = createModel(context, () => NavLinkTitleModel());
    navLinkTitleModel5 = createModel(context, () => NavLinkTitleModel());
    navLinkTitleModel6 = createModel(context, () => NavLinkTitleModel());
    navLinkTitleModel7 = createModel(context, () => NavLinkTitleModel());
    navLinkTitleModel8 = createModel(context, () => NavLinkTitleModel());
  }

  @override
  void dispose() {
    navLinkTitleModel1.dispose();
    navLinkTitleModel2.dispose();
    navLinkTitleModel3.dispose();
    navLinkTitleModel4.dispose();
    navLinkTitleModel5.dispose();
    navLinkTitleModel6.dispose();
    navLinkTitleModel7.dispose();
    navLinkTitleModel8.dispose();
  }
}
